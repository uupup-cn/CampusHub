# 部署问题记录与解决方案

> 本文档记录 CampusHub 项目部署过程中遇到的全部问题、根因分析和解决方案，供后续部署排障参考。
> 最后更新：2026-08-11

---

## 一、Discourse Bootstrap 失败

Discourse Docker 容器 bootstrap 共失败 4 次，涉及三个独立根因。

### 问题 1：register_asset 废弃导致 Rails 启动崩溃

**现象**

bootstrap 在 db:migrate 阶段失败，日志关键错误：

    Javascript files under assets/javascripts are automatically included in JS bundles.
    Manual register_asset calls should be removed. (attempted to add javascripts/chb_checkin.js)

**根因**

plugin.rb 中调用了 register_asset "javascripts/chb_checkin.js"。新版 Discourse（3.x+）中，assets/javascripts/ 目录下的 JS 文件会自动纳入 JS bundle，手动 register_asset 不仅多余，还会被作为硬错误抛出，导致 Rails 加载失败，进而 db:migrate 无法执行。

**解决方案**

从 plugin.rb 中移除 register_asset "javascripts/chb_checkin.js" 这一行。CSS 的 register_asset "stylesheets/chb_checkin.css" 保留（CSS 仍需手动注册）。

**提交**：PR #15

---

### 问题 2：require_dependency 在 Zeitwerk 下找不到文件

**现象**

修复问题 1 后，bootstrap 仍在 db:migrate 阶段失败，日志关键错误：

    Plugin name is chb_reward_plugin, but plugin directory is named chb-reward-plugin
    rake aborted!
    LoadError: cannot load such file -- chb_event_handler (LoadError)
    /var/www/discourse/plugins/chb-reward-plugin/plugin.rb:12

**根因**

新版 Discourse 使用 Zeitwerk 自动加载机制。require_dependency "chb_event_handler" 通过 ActiveSupport::Dependencies 系统查找文件，但 Zeitwerk 下插件 lib/ 目录的加载路径注册依赖插件名正确解析（见问题 3）。即使插件名正确，Zeitwerk 对文件名与常量名的映射规则更严格，require_dependency 在 after_initialize 块中不可靠。

**解决方案**

将 plugin.rb 中的 require_dependency 全部改为 require_relative。

修改前：

    require_dependency "chb_event_handler"
    require_dependency "chb_api_client"
    require_dependency "chb_event_serializer"
    require_dependency "chb_trust_level_sync"

修改后：

    require_relative "lib/chb_event_handler"
    require_relative "lib/chb_api_client"
    require_relative "lib/chb_event_serializer"
    require_relative "lib/chb_trust_level_sync"

require_relative 基于调用文件（plugin.rb）的路径解析，不依赖 Zeitwerk 的加载路径注册，更可靠。

**提交**：PR #15

---

### 问题 3：UTF-8 BOM 导致插件名解析为空

**现象**

问题 2 修复后，日志中插件名显示为空字符串：

    Plugin name is (empty), but plugin directory is named chb-reward-plugin

这导致 Discourse 无法将插件 lib/ 目录注册到加载路径，require_dependency 找不到文件。

**根因**

plugin.rb 文件开头有 UTF-8 BOM（Byte Order Mark，EF BB BF）。Discourse 通过解析 plugin.rb 注释头中的 # name: chb_reward_plugin 来获取插件名。BOM 字符使 YAML 解析器无法识别 # name: 注释行，导致插件名为空。

BOM 来源：Windows 上 PowerShell 的 Set-Content -Encoding UTF8 默认添加 BOM；apply_patch 工具也可能引入 BOM。

**解决方案**

清除所有插件文件的 BOM。PowerShell 命令：

    $bytes = [System.IO.File]::ReadAllBytes("path/to/file.rb")
    if ($bytes[0] -eq 0xEF -and $bytes[1] -eq 0xBB -and $bytes[2] -eq 0xBF) {
        [System.IO.File]::WriteAllBytes("path/to/file.rb", $bytes[3..($bytes.Length-1)])
    }

对以下 6 个文件全部执行：
- plugin.rb
- config/settings.yml
- lib/chb_event_handler.rb
- lib/chb_api_client.rb
- lib/chb_event_serializer.rb
- lib/chb_trust_level_sync.rb

**预防措施**

- 所有 Ruby/YAML 文件必须使用 UTF-8 无 BOM 编码
- PowerShell 中使用 [System.IO.File]::WriteAllText 写文件，配合 UTF8Encoding($false) 参数
- 提交前用 xxd 或 ReadAllBytes 检查文件头

**提交**：PR #15

---

### 问题 4：settings.yml 后端端口不匹配

**现象**

插件设置 chb_backend_api_url 默认值为 http://localhost:8080，但后端实际运行在端口 9090。

**解决方案**

将 config/settings.yml 中 chb_backend_api_url 的默认值从 http://localhost:8080 改为 http://localhost:9090。

**提交**：PR #15

---

## 二、容器内 Rails 命令执行问题

### 问题 5：PostgreSQL Peer 认证失败

**现象**

以 root 身份在 Discourse 容器内执行 bin/rails runner 时，报错：

    FATAL: Peer authentication failed for user "discourse"

**根因**

Discourse 内置 PostgreSQL 使用 peer 认证：连接的操作系统用户必须与数据库用户名匹配。以 root 运行时，OS 用户 root 与数据库用户 discourse 不匹配，认证被拒绝。

**解决方案**

使用 docker exec --user discourse 指定以 discourse 用户身份运行命令：

    docker exec --user discourse -e RAILS_ENV=production -w /var/www/discourse app /var/www/discourse/bin/rails runner /shared/setup.rb

关键参数：
- --user discourse：以 discourse 用户运行，解决 peer 认证
- -e RAILS_ENV=production：设置 Rails 环境
- -w /var/www/discourse：设置工作目录
- 使用完整路径 /var/www/discourse/bin/rails，避免 PATH 查找问题

---

### 问题 6：su 命令无法正确切换工作目录

**现象**

docker exec app su discourse -c "cd /var/www/discourse && bin/rails ..." 报错 cd: /var/www/discourse: No such file or directory，但 docker exec app ls /var/www/discourse/ 可以正常列出目录内容。

**根因**

su discourse -c "..." 在某些 Docker 环境下，shell 初始化过程与 cd 命令存在兼容性问题。su 不加 -（login shell）时不加载用户 profile，PATH 可能缺少必要路径。加 - 后切换到用户 home 目录，但后续 cd 行为仍不稳定。

**解决方案**

放弃 su 方式，改用 docker exec --user discourse 直接指定运行用户（见问题 5 的解决方案）。这是 Docker 原生方式，更可靠。

---

### 问题 7：Discourse 管理员密码长度限制

**现象**

创建管理员账号时报错 Password is too short (minimum is 15 characters)。

**根因**

Discourse 默认要求密码至少 15 个字符。

**解决方案**

将密码从 CampusHub2026（13 字符）改为 CampusHubAdmin2026!（20 字符）。

---

### 问题 8：SiteSetting.site_name 不存在

**现象**

配置站点设置时报错 undefined method site_name= for class SiteSetting。

**根因**

新版 Discourse 没有 site_name 设置项，站点名称通过 SiteSetting.title 配置。

**解决方案**

移除脚本中的 SiteSetting.site_name 和 SiteSetting.description 行，只保留 SiteSetting.title。

---

## 三、SSH 与 PowerShell 环境问题

### 问题 9：SSH 后台进程导致连接挂起

**现象**

通过 SSH 在服务器上启动后台 bootstrap 进程时，SSH 连接挂起不返回，最终超时。

**根因**

nohup ./command & 中的后台进程继承了 SSH 的 stdout/stderr 文件描述符。即使命令本身已完成，SSH 仍会等待这些描述符关闭。

**解决方案**

写一个独立的 shell 脚本文件，SCP 到服务器，然后用 nohup 配合 /dev/null 重定向执行：

    # 本地写脚本 run_bootstrap.sh
    #!/bin/bash
    cd /var/discourse
    ./launcher bootstrap app

    # SCP 到服务器后执行
    ssh devbox 'nohup /tmp/run_bootstrap.sh > /tmp/discourse_bootstrap.log 2>&1 < /dev/null & echo PID=$!'

关键点：
- < /dev/null 重定向 stdin，断开与 SSH 的关联
- 输出重定向到日志文件，不通过 SSH 传输
- screen 和 tmux 在嵌套引号场景下不可靠

---

### 问题 10：Windows 路径反斜杠触发 JavaScript 转义

**现象**

在 exec 工具的 JavaScript 模板字符串中，Windows 路径中的反斜杠加 x 被解析为 JavaScript 十六进制转义序列，报错 Invalid hexadecimal escape sequence。

**根因**

exec 工具运行在 JavaScript V8 isolate 中，模板字符串中的反斜杠 x 是合法的十六进制转义前缀。如果后面不是合法十六进制数字（0-9, a-f, A-F），解析失败。

**解决方案**

- 在传递给 shell_command 的路径中使用正斜杠（PowerShell 同时支持）
- 或在 JavaScript 字符串中使用双反斜杠转义

---

### 问题 11：PowerShell 不支持 bash 的 || 运算符

**现象**

在 PowerShell 命令中使用 command1 || command2 报错。

**解决方案**

PowerShell 中使用 try/catch 替代：

    try { Invoke-WebRequest -Uri "..." -TimeoutSec 5 } catch { "ERROR" }

---

### 问题 12：gh api 分支保护恢复时类型转换错误

**现象**

使用 gh api -f 恢复分支保护时，GitHub API 返回 422 错误：true is not a boolean。

**根因**

gh api -f 将所有值作为字符串传递，但 GitHub API 要求 strict、enforce_admins 等为 boolean，required_approving_review_count 为 integer。

**解决方案**

使用 Invoke-RestMethod 直接调用 REST API，传入正确类型的 JSON body。

---

## 四、前端问题

### 问题 13：Navbar 显示模拟用户而非登录入口

**现象**

前端导航栏右上角显示"用户 U1"和"用户 U2"切换按钮，点击不跳转登录。

**根因**

开发阶段前端通过 X-User-ID 请求头模拟用户身份，Navbar.tsx 中的 switchUser() 在 U1（ID=1）和 U2（ID=2）之间切换，存储在 localStorage 中。

**解决方案**

- 移除 switchUser() 函数和模拟用户逻辑
- 替换为"登录"按钮，点击跳转 Discourse 登录页
- 新增"论坛"导航链接，直接打开 Discourse 首页
- 移除 useState、getUserID、setUserID 的导入

**提交**：PR #16

---

### 问题 14：前端中文显示乱码

**现象**

Navbar.tsx 中的中文标签显示为乱码（如 闆嗗競 而非 集市）。

**根因**

文件编码不一致。文件以 UTF-8 BOM 保存，但 PowerShell Get-Content 在某些情况下以系统默认编码（GBK）读取，导致显示乱码。实际文件内容可能是正确的。

**解决方案**

使用 [System.IO.File]::WriteAllText 配合 UTF8Encoding($false) 重写文件，确保 UTF-8 无 BOM 编码。

---

## 五、部署流程经验总结

### Discourse 部署正确流程

1. 准备 app.yml，确保不挂载 uploads 卷（与容器内 symlink 冲突）
2. 设置 DISCOURSE_SKIP_EMAIL_SETUP 为 1，跳过 SMTP 检查
3. 插件文件全部使用 UTF-8 无 BOM 编码
4. 插件 plugin.rb 使用 require_relative 而非 require_dependency
5. 不要 register_asset JS 文件
6. bootstrap 成功后执行 ./launcher start app 启动容器
7. 等待 30 秒让 Rails/Unicorn 完全启动
8. 用 docker exec --user discourse 创建管理员和配置设置

### 服务器环境

| 角色 | 服务器 | 说明 |
|------|--------|------|
| 部署测试 | 112.213.106.104:33848 | Ubuntu, Docker, systemd 管理所有服务 |
| 开发环境 | 本机 WSL2 | Go 1.25 + Node 22, SSH 免密登录服务器 |

### 服务端口映射

| 服务 | 内部端口 | 外部端口 | Nginx 路由 |
|------|----------|----------|------------|
| Go 后端 | 9090 | 通过 Nginx | /api/ 指向 9090 |
| Next.js 前端 | 3000 | 通过 Nginx | / 指向 3000 |
| Discourse | 9800 | 9800 直通 | /forum/ 指向 9800 |
| PostgreSQL 平台 | 5432 | Docker 内部 | chb-postgres 容器 |
| PostgreSQL Discourse | 容器内 | 不暴露 | Discourse 自带 |

### 文档引用

- 部署配置详情参见 deploy/discourse/app.yml
- 初始化脚本参见 deploy/discourse/setup.rb
- Bootstrap 脚本参见 deploy/discourse/run_bootstrap.sh
- 开发规则与编码规范参见项目根目录 AGENT.md
- API 路由设计参见 docs/02-API文档.md
- 开发计划参见 docs/03-开发计划.md
